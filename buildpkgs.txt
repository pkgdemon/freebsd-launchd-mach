# One pkg per line; lines starting with # are ignored.
# Build-only packages — installed in the chroot to compile our
# vendored sources (libdispatch via cmake), then purged before the
# slim/mkuzip pass so they don't ship in the ISO.
cmake
ninja
# CMake's find_package module-mode probes use pkg-config under the
# hood — keep pkgconf so libdispatch's cmake discovery works.
pkgconf
